import { Behavior } from "@dreamlab/engine";

export default class LookAtMouse extends Behavior {
  onPostTick() {
    // TODO: If we move the face the cursor code in here it works but it shouldn't!
  }
}
